import os

from dotenv import load_dotenv

load_dotenv()

# Минимальный спред для поиска (%)
MIN_SPREAD = 0.01

# Минимальный спред для отображения (%)
DISPLAY_SPREAD = 0.50

# Минимальный объем (USDT)
MIN_VOLUME_USDT = 500_000

# Минимальный funding-спред для поиска (%), за один период
MIN_FUNDING_SPREAD = 0.01

# Сколько уровней стакана запрашивать при проверке проскальзывания
DEPTH_LIMIT = 50

# Спред выше этого порога (%) почти наверняка означает не реальный арбитраж,
# а ошибку данных или два разных актива под одинаковым тикером на разных биржах
MAX_SANE_SPREAD = 20

# Ключ CoinGecko Demo API (бесплатный, https://www.coingecko.com/en/api/pricing).
# Берётся из переменной окружения/.env — никогда не хранится в коде.
# Если не задан, проверка идентичности актива (CoinIdentityChecker) отключается.
COINGECKO_API_KEY = os.environ.get("COINGECKO_API_KEY", "")

# Лимит запросов к CoinGecko в минуту. Консервативно — под нижнюю границу
# бесплатного Demo-тарифа (в разных источниках заявлено от 30 до 100/мин).
# При полном скане рынка (без фильтра монет) проверка идентичности может
# занять несколько минут — это ограничение биржи, а не наша производительность.
COINGECKO_RATE_LIMIT_PER_MINUTE = 25

# Размер позиции (USDT)
POSITION_SIZE_USDT = 1000

# Фильтр монет: [] = весь рынок, иначе например ["BTC", "ETH", "SOL"]
FILTER_COINS = []

# Биржи
EXCHANGES = {
    "binance": {
        "enableRateLimit": True,
        "options": {"defaultType": "future"},
    },
    "bybit": {
        "enableRateLimit": True,
        "options": {"defaultType": "linear"},
    },
    "mexc": {
        "enableRateLimit": True,
        "options": {"defaultType": "swap"},
    },
}

# Путь к SQLite базе истории найденных возможностей
HISTORY_DB_PATH = "data/scanner.db"

# Шортить спот (продавать актив, которого нет) обычным способом нельзя —
# нужен маржинальный/кредитный аккаунт (доступен не везде и не под все
# монеты, плюс проценты по займу мы не считаем). Поэтому по умолчанию
# сделки, где "продажная" нога — спот, исключаются из выдачи.
# Включай True, только если реально настроил маржинальный шорт спота.
ALLOW_SHORT_SPOT = False
